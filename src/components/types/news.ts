export interface News {
    id: string;
    title: string;
    content: string;
    headline: string;
    createdAt: string;
    updateAt: string;
    author: string;
    authorId: string;
    image?: string;
}
